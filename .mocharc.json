{
  "ui": "bdd",
  "reporter": "tap",
  "recursive": true,
  "fullTrace": true,
  "spec": ["test/specs/*.specs.ts", "test/specs/**/*.specs.ts"],
  "require": ["cross-fetch/polyfill", "isomorphic-form-data"]
}
